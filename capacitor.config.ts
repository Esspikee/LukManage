import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.esspikee.lukmanage",
  appName: "LukManage",
  webDir: "dist",
  android: {
    backgroundColor: "#171721",
  },
};

export default config;
